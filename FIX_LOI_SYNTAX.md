# Sửa lỗi cú pháp C# / Razor

Bản này đã thay các collection expression C# mới (`[]`) bằng cú pháp cổ điển và đơn giản hóa biểu thức trong thuộc tính Razor.

Sau khi ghi đè file:

1. Đóng Visual Studio.
2. Xóa thư mục `TMDT1_TH/bin` và `TMDT1_TH/obj` nếu có.
3. Mở lại solution.
4. Chọn **Build → Rebuild Solution**.

Không cần tạo migration mới.
